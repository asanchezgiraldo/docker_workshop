pipeline {
    stages {
        stage('build') {
            steps {
            // some block
            }
        }
        stage('test) {
              steps{
              // some block
              }
        }
    }
}
